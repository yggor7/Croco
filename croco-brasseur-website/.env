EMAIL_USER=contact@lecrocobrasseur.com
   EMAIL_PASS=xxxx xxxx xxxx xxxx
```

### 📊 Base de Données - Tables Créées :

| Table | Description |
|-------|-------------|
| `reservations` | Toutes les réservations clients |
| `contacts` | Messages du formulaire de contact |
| `evenements` | Événements (Karaoke, Open Mic, etc.) |
| `newsletter` | Emails inscrits à la newsletter |
| `catering_requests` | Demandes de catering/événements privés |
| `bieres` | Catalogue des bières artisanales |

### 🌐 API Endpoints Disponibles :
```
POST   /api/reservations      → Créer une réservation
GET    /api/reservations      → Liste des réservations
POST   /api/contact           → Envoyer un message
POST   /api/newsletter        → S'inscrire à la newsletter
GET    /api/events            → Liste des événements
POST   /api/catering          → Demande de catering
GET    /api/health            → Vérifier le serveur